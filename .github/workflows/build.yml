
name: Build Python Executable

on: [push]
  

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2

      
      
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'
      
      - name: Install dependencies
        run: |
          pip install pyinstaller
      
      - name: Build executable
        run: pyinstaller --onefile --name WinProgramLister app.py
      - name: executa o arquivo spec
        run: pyinstaller WinProgramLister.spec
      - name: lista o diretorio
        run: |
          dir
          dir dist
          dir build

      - name: move o exe
        run: move dist\WinProgramLister.exe.

      - name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: /dist/WinProgramLister.exe
          tag_name: 'release'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
